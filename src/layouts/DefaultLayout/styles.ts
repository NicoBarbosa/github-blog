import styled from 'styled-components'

export const LayoutContainer = styled.div`
  > img {
    position: relative;
    z-index: -1;
  }

  main {
    width: 100%;
    max-width: 54rem;
    margin: -5.5rem auto 14.62rem;
  }
`
